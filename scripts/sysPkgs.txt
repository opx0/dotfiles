--> Fonts (active set — installed by package manager)
# Keep this list lean. Each installed font slows fontconfig and app startup.
# Add more only when you wire them into a config (kitty/ghostty/wezterm/nvim).
ttf-jetbrains-mono-nerd
ttf-nerd-fonts-symbols
ttf-nerd-fonts-symbols-mono
noto-fonts

--> Fonts (paid / proprietary)
# Not packageable. Live at /usr/local/share/fonts/personal/ (system-wide).
# Restore by hand from your private backup, then run: fc-cache -fv

--> General Linux Utilities
brightnessctl
gvfs
gvfs-mtp
zsh
nushell
reflector
gparted
flameshot
ark

--> X11
touchegg
xdotool
scrot
xclip
#clipsync

--> Wayland
nwg-look
#wl-screenrec
#ydotool
#wl-copy
#wl-paste
#cliphist
#wl-x11-clipsync
#grim

--> ...
libinput-gestures
ydottool

--> Voice input (voxtype push-to-talk)
# voxtype-bin is AUR. It pulls ~300MB of CUDA/MIGraphX ONNX runtimes a Vulkan
# setup never loads -- not optional in the PKGBUILD. Don't build on a small tmpfs.
voxtype-bin
# wtype is first in voxtype's driver_order; without it typing falls to ydotool.
wtype
wl-clipboard
libnotify
vulkan-icd-loader
# espeak-ng: only needed to self-test transcription (synthesise a known phrase).
espeak-ng
